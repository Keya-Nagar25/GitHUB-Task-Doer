# GitHub Task Doer — VS Code Extension

VS Code extension (TypeScript) that executes resolved actions locally against the open workspace. Not yet scaffolded — built in Milestone 6 of `docs/architecture.md`.
